{
  "psram": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "enable_ecc": {
              "default": "False",
              "docs": "**, bool**: For octal mode, enable ECC (Error Correction Code) for the PSRAM (default is off.) ECC is a method of detecting and correcting single-bit errors in memory. It will reduce the available PSRAM size and speed by 1/16th, but also increases the rated temperature range of some ESP32 modules.\n\n*See also: [PSRAM](https://esphome.io/components/psram.html#configuration-variables)*",
              "key": "Optional",
              "type": "boolean"
            },
            "id": {
              "id_type": {
                "class": "psram::PsramComponent",
                "parents": [
                  "Component"
                ]
              },
              "key": "GeneratedID"
            },
            "mode": {
              "default": "quad",
              "docs": "Defines the operating mode the PSRAM should utilize. One of `quad` (default) or `octal`.\n\n*See also: [PSRAM](https://esphome.io/components/psram.html#configuration-variables)*",
              "key": "Optional",
              "type": "enum",
              "values": {
                "octal": null,
                "quad": null
              }
            },
            "speed": {
              "default": "40000000.0",
              "docs": "**, int**: The speed at which the PSRAM should operate. One of `40MHz` (default), `80MHz` or `120MHz`.\n\n*See also: [PSRAM](https://esphome.io/components/psram.html#configuration-variables)*",
              "key": "Optional",
              "type": "enum",
              "values": {
                "120000000.0": null,
                "40000000.0": null,
                "80000000.0": null
              }
            }
          }
        },
        "type": "schema"
      }
    }
  }
}