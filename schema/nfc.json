{
  "nfc.binary_sensor": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "id": {
              "id_type": {
                "class": "nfc::NfcTagBinarySensor",
                "parents": [
                  "Component",
                  "EntityBase",
                  "Parented",
                  "Parented<nfc::Nfcc>",
                  "binary_sensor::BinarySensor",
                  "nfc::NfcTagListener"
                ]
              }
            },
            "ndef_contains": {
              "docs": "**, string**: A (sub)string that must appear in the tag\u2019s NDEF message. May not be used with `tag_id` and/or `uid` (below).\n\n*See also: [NFC Binary Sensor](https://esphome.io/components/binary_sensor/nfc.html#configuration-variables)*",
              "key": "Optional",
              "type": "string"
            },
            "nfcc_id": {
              "key": "GeneratedID",
              "type": "use_id",
              "use_id_type": "nfc::Nfcc"
            },
            "tag_id": {
              "docs": "**, string**: A string that identifies the tag; in effect, its name. Specifically, this looks for the Home Assistant URI encoded into one of the tag\u2019s NDEF records and then looks for this specific string. May not be used with `ndef_contains` and/or `uid`.\n\n*See also: [NFC Binary Sensor](https://esphome.io/components/binary_sensor/nfc.html#configuration-variables)*",
              "key": "Optional",
              "type": "string"
            },
            "uid": {
              "docs": "**, string**: The unique ID of the NFC tag. This is a hyphen-separated list of hexadecimal values. For example: `74-10-37-94`. May not be used with `ndef_contains` and/or `tag_id` (above).\n\n*See also: [NFC Binary Sensor](https://esphome.io/components/binary_sensor/nfc.html#configuration-variables)*",
              "key": "Optional"
            }
          },
          "extends": [
            "binary_sensor.BINARY_SENSOR_SCHEMA",
            "core.COMPONENT_SCHEMA"
          ]
        },
        "type": "schema"
      }
    }
  }
}