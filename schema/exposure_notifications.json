{
  "exposure_notifications": {
    "schemas": {
      "CONFIG_SCHEMA": {
        "schema": {
          "config_vars": {
            "on_exposure_notification": {
              "docs": "**, [Automation](https://esphome.io/automations/index.html#automation)**: An automation to run when an exposure notification bluetooth message is received.A variable `x` of type :apistruct:`exposure_notifications::ExposureNotification` is passed to the automation.\n\n*See also: [Exposure Notification Listener](https://esphome.io/components/exposure_notifications.html#configuration-variables)*",
              "key": "Required",
              "schema": {
                "config_vars": {
                  "esp32_ble_id": {
                    "key": "GeneratedID",
                    "type": "use_id",
                    "use_id_type": "esp32_ble_tracker::ESP32BLETracker"
                  },
                  "then": {
                    "type": "trigger"
                  }
                }
              },
              "type": "trigger"
            }
          }
        },
        "type": "schema"
      }
    }
  }
}